#include <iostream>
using namespace std;
using ll = long long;
const double r0=40.0;
const double k=0.04;
void score_output(int h,int w,vector<int> &windx,vector<int> &windy,vector<vector<int>> windp) {
    double ficore=0.0;
    int rss=0;
    for(int i=0;i<windx.size();i++) {
        for(int j=0;j<windx.size();i++) {
            if(i==j) continue;
            
        }
    }
}
int main(void){
    
}
